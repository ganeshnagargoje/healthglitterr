[global]
# Prefer binary wheels to avoid compilation
prefer-binary = true

# For packages known to have wheel issues, only use binary
# Note: PyMuPDF excluded because paddleocr requires old version without wheels
only-binary = numpy,opencv-python,opencv-contrib-python,paddlepaddle,scikit-image,Pillow
