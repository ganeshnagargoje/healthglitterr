[global]
# Windows pip configuration
# Prefer binary wheels to avoid compilation issues
prefer-binary = true

# Only use binary wheels for packages that are problematic to build on Windows
# Note: PyMuPDF excluded because paddleocr requires old version without wheels
only-binary = numpy,opencv-python,opencv-contrib-python,paddlepaddle,scikit-image,Pillow,lxml,cython
